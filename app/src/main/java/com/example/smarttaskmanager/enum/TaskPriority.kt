package com.example.smarttaskmanager.enum

enum class TaskPriority(val displayName: String) {
    SELECT("Not Selected"),
    HIGH("High Priority"),
    MEDIUM("Medium Priority"),
    LOW("Low Priority")
}
